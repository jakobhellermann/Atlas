<Project Sdk="WixToolset.Sdk/4.0.3">
    <PropertyGroup>
        <OutputName>tbd</OutputName>
    </PropertyGroup>

    <Target Name="Publish App" BeforeTargets="BeforeBuild">
        <Exec Command="cargo build --release --manifest-path ../Cargo.toml --out-dir bin -Zunstable-options"/>
    </Target>
</Project>